export const environment = {
  production: true,
  api_url:'/',
  options: {
      timeOut: 5000,
      showProgressBar: true,
      pauseOnHover: false,
      clickToClose: false,
      maxLength: 100
    }
};